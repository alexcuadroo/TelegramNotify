telegram:
  token: "TU_BOT_TOKEN"
  chat_id: "TU_CHAT_ID"

# Activar/desactivar tipos de notificaciones
notifications:
  join: true
  quit: true
  death: true
  server_start: true

# Mensajes personalizables
# Placeholders disponibles:
#   {player} - Nombre del jugador
#   {death_message} - Mensaje completo de muerte (solo en death)
#   {location} - Coordenadas X, Y, Z (solo en death)
#   {world} - Nombre del mundo (solo en death)
#   {x}, {y}, {z} - Coordenadas individuales (solo en death)
messages:
  join: "🟢 *{player}* entró al servidor"
  quit: "🔴 *{player}* salió del servidor"
  death: "💀 {death_message}\n📍 Ubicación: `{location}` ({world})"
  server_start: "🔵 Servidor iniciado"
