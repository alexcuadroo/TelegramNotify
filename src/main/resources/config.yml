telegram:
  token: "TU_BOT_TOKEN"
  chat_id: "TU_CHAT_ID"

# Activar/desactivar tipos de notificaciones
notifications:
  join: true
  quit: true
  death: true
  server_start: true

# Mensajes personalizables ({player} será reemplazado por el nombre del jugador)
messages:
  join: "🟢 *{player}* entró al servidor"
  quit: "🔴 *{player}* salió del servidor"
  death: "✖️ *{player}* murió"
  server_start: "🔵 Servidor iniciado"
